# 关于类脑智能的一些思考

## 一、从 Transformer 的结构说起

这一切思考的起点，是我在琢磨一个很朴素的问题：现在的大模型内部结构，到底是"立体"的还是"平面"的？

后来搞清楚了：Transformer 是"整体立体堆叠 + 局部平面并行"——多个 Transformer 块自下而上层层堆叠，是立体的；但同一层内的多头注意力，是并行、独立、互不干涉的分支，本质是平面的。这和人脑那种一个神经元向四面八方伸出上千甚至上万个突触、彼此深度交织的连接方式完全不同。Transformer 的连接是"规则化、局部化"的：同层多头参数互不交叉，层与层之间只有线性传递，注意力也只是 token 与 token 之间的成对关联。

这让我意识到一个关键问题：**Transformer 的连接效率其实并不高**。它靠"任务导向的密集计算"堆出关联能力——比如自注意力要算所有 token 对的相似度，看起来连接很多，但真正有效的部分早被 softmax 压缩了；而多头之间的并行隔离，又浪费了跨头协同的潜力。相比之下，人脑是"稀疏但关键连接强"，能用极低的能耗实现更灵活的关联。这是我对现有大模型架构产生怀疑的第一个入口。

## 二、大模型的"记忆"和人脑的"记忆"不是一回事

顺着这个思路往下想，我发现大模型的另一个根本局限是：**参数训练完就锁死了**。新信息只能塞进上下文窗口里临时存一下，对话一关就忘，本质上只有"缓存"，没有"实时更新的长期记忆"。而人脑的参数——也就是突触连接强度——是随时都在微调的，就像"热加载"一样。

反向传播之所以难以支撑这种实时更新，是因为它需要全局计算梯度，代价太高，和人脑局部突触的快速调整完全不是一个逻辑。目前能想到的替代方向包括：局部学习规则（只更新受影响的局部参数）、外部记忆模块（单独存新知识，推理时动态调取，不改动主模型）、脉冲神经网络（用脉冲时间差模拟突触的实时调整）。但这些要么还不够稳定，要么工程上很难落地。

这也让我进一步去想"记忆"本身的机制问题——我曾设想过用知识图谱来做一个可以动态更新、多跳查询的记忆系统，后来发现更现实的方案是"知识图谱 + 向量数据库"的混合架构：图谱负责精确的多跳关联，向量库负责边缘的模糊匹配和快速初筛，大模型在中间做调度。而人脑的记忆系统其实也是混合的——情景记忆像带时间戳的子图谱，语义记忆是抽象化的关系网络，程序记忆则更像向量式的模式匹配，提取一个概念时会同时激活多个系统并行工作。这种"多通道协同"，恰恰是现有大模型单一统计记忆所缺的。

## 三、"记忆"和"理解"的边界在哪里

一个很有意思的追问是：像"天空是蓝色的"这种常识，到底算记忆还是算理解？

我的结论是：人脑的常识记忆是**绑定了视觉体验和场景关联的概念记忆**——遇到黄昏的天空，能立刻切换认知，因为背后有"光线决定颜色"的因果概念在支撑；而大模型的记忆是孤立的统计条目，没见过的场景就容易卡壳，因为它学的是"文本共现概率"，而不是因果推理。人脑其实有"双通道"：熟悉场景走记忆捷径省能量，遇到新场景就切换到概念/感官通道去灵活推理。现有大模型只有统计记忆这一条路，没法像人脑那样按需切换。

这也解释了为什么人脑"回忆"特别快——它是**关联网络式的并行激活**，一个线索能瞬间点亮一整片相关神经元集群；而模型的"回忆"是串行地在参数里查表匹配，天然就慢。

## 四、语音输出：人脑靠肌肉，模型靠波形

在探讨多模态模型如何"听懂并说出话"时，我发现一个很有意思的对比：人脑从来不会直接"吐文字"，它的一切抽象思考最终都要落地成对身体（声带、口腔肌肉）的精确物理控制，这是进化优先级决定的——先有运动控制能力，后来才在此基础上叠加语言功能。而大模型是为"信息传递"设计的，直接在高维向量空间里做全局计算，生成文字或波形，天然就是重计算、高功耗的路子。

由此我意识到，现有大模型（包括语音生成）的高功耗，本质上是"设计目标"带来的先天短板：为了覆盖通用场景，把所有语义都映射到统一的高维空间，每次输出都要全局计算；而人脑是"任务专用化"的，处理语言时只激活特定环路，其他区域直接休眠，功耗自然低。这条思路把我引向了整篇思考里最核心的技术命题——**怎样才能做出一个功耗低、事件驱动的类脑系统**。

## 五、神经元连接的"远距离通路"与个体独特性

在了解人脑连接方式时，一个细节让我印象很深：神经元之间的连接不只是就近连接，还存在大量跨脑区的远距离连接（比如额叶到颞叶），这类连接更像大脑的"信息高速公路"，一旦受损，影响是全局性的（语言、决策、情绪调控整体紊乱），而不是局部小功能缺失。

这些远距离连接的基础框架是基因预先编码好的（胚胎期轴突沿分子信号通路精确生长到靶区域），但后天会持续修剪、强化，所以**每个人的大脑连接细节都是独一无二的**——基础脑区（脑干、丘脑）相对保守，但前额叶、颞叶这些高级认知区域，因为经历不同，连接的强度和靶点会产生巨大的个体差异，哪怕同卵双胞胎也不例外。

这也是为什么果蝇的完整大脑连接组（约14万神经元、上千万突触）可以被扫描并复现，而人脑（860亿神经元、上百万亿突触）目前只能做到局部脑区的精细图谱——量级和复杂度差了好几个数量级。

## 六、果蝇大脑仿真实验带来的关键启发

这次讨论中一个很触动我的具体案例，是 2024 年科学家把完整的果蝇大脑连接组（结构数据）导入仿真环境，用相对简化的脉冲神经元模型（LIF 模型）去驱动，结果虚拟果蝇能"自发"表现出觅食、避障等行为——**这些行为不是手写规则，而是从真实连接结构里自然涌现出来的**。

深入了解后我理清了几个关键点：

- **结构是真实的，计算是简化的**：连接图（谁连谁、强度多少、兴奋还是抑制）来自真实电镜扫描，但每个神经元的动力学用的是简化模型。
- **计算范式和大模型完全不同**：不是密集矩阵乘法，而是事件驱动的稀疏计算——只有神经元电位超过阈值"放电"时才产生计算和通信；没有反向传播，跑的是已经构建好的固定网络。
- **输出的本质不是概率分布**：大模型最终输出的是一个概率分布，再采样出 token；而脉冲网络输出的是"哪些神经元放电、什么时候放电、频率多高"，直接驱动行为，概率是观察者事后统计出来的产物，不是系统本身的输出形式。
- **这套仿真目前还没有学习能力**：真实果蝇是能学习的（比如把气味和电击关联起来学会躲避），但目前的仿真更多是"把长好的大脑复制一份跑起来"，产生固定行为模式，并没有把突触可塑性（学习机制）真正嵌入进去。

这个案例让我意识到：**光有正确的连接结构，还不足以构成"类脑"**，必须同时具备能实时改变自身的学习机制，否则就只是一个复杂但静态的行为发生器。

## 七、类脑研究到底卡在哪里

综合以上所有线索，我把类脑智能落地的瓶颈归纳为三个相互制约的方面：

1. **模型/连接结构**：果蝇级别的连接组勉强能做到，但人脑规模的完整连接图谱，以现有技术还差得很远；
2. **学习机制**：这是最核心的难点。反向传播高效但不符合生物逻辑，也没法支撑实时更新；真正接近生物的局部可塑性学习规则，目前还没有能扩展到大规模网络、又足够高效稳定的方案；
3. **硬件基础**：GPU 为密集矩阵运算而生，天然不适合脉冲网络这种稀疏、事件驱动的计算方式。这就引出了**存算一体**这个我认为最有希望的方向——把存储和计算融合在同一个物理单元里，数据不用来回搬运，功耗能降低一到两个数量级。人脑本质上就是"存算一体"的：突触既存权重，又直接参与信号的加权计算，从来没有分开过。而现有 GPU 架构里，跑大模型时，真正花在矩阵计算上的功耗其实占比不高，六七成甚至更高的功耗都耗在了数据在显存、缓存、计算单元之间来回搬运上——这是冯诺依曼架构的天生缺陷，也是英伟达很难在现有 GPU 上"顺手"改造出存算一体方案的原因，因为这需要推翇整个架构和生态重来。

而这三个方向必须协同推进：脉冲类脑算法要配合存算一体硬件的物理特性去设计，不是简单地把现有深度学习模型搬过去就能用；硬件流片前，也可以先在仿真平台（如 NeuroSim、CIMulator、CrossSim）里把结构、脉冲算法、功耗预估都跑通，再进入实际制造流程，这也是目前前沿实验室的通行做法。基于此，我判断：十年内可能出现更成熟的神经形态硬件和小规模持续学习的进展，但要做出接近人脑水平、能自主持续学习的通用类脑系统，大概率还需要十五到二十年。

## 八、如果类脑真的做出来，意味着什么

顺着这个方向想下去，我越来越觉得，如果类脑系统真的成熟，它带来的冲击会远超 ChatGPT 这一代大模型。因为它不再是被动消化文本统计规律的工具，而是能像婴儿一样，通过真实的感官输入（视觉、听觉都可以先转成电信号/脉冲序列，再喂给网络）在世界里自主试错、积累经验、持续成长——这恰好是当下机器人最大的短板：现在的机器人"大脑"基本靠人工编码的规则和控制算法，换个场景就失灵，本质上没有真正意义上的学习能力。类脑一旦成熟并装到机器人身上，机器人才真正有可能从"专用设备"跃迁为"通用智能体"。

这样的突破，究竟会诞生在资源雄厚的顶尖实验室，还是某个跳出既有框架束缚、找到简洁突破口的个人手中，我倾向于认为：核心的软硬件协同（脉冲算法必须为存算一体硬件的物理特性量身定制，两者深度绑定，不像"大模型 + GPU"那样可以自由适配、随意迁移）决定了这更可能需要跨学科团队和长期资源投入，但历史上确实不乏个人凭借一个关键的、被主流忽视的思路完成颠覆性突破的先例。无论最终由谁完成，这一步都相当于人类第一次亲手创造出具备自主感知和自主学习能力的新智能物种，其分量足以匹配任何最高等级的科学荣誉。

## 九、从技术思考照回自身

想着想着，我发现这套"类脑可塑性"的逻辑，其实也照见了我自己。人脑的可塑性意味着：你经常做什么，大脑就会被"训练"成更适合做什么。如果长期习惯刷短视频这种碎片化、高刺激的内容，大脑其实就被悄悄"调参"成了适应短平快反馈的状态；等真正需要静下心做长时间深度思考或学习时，注意力反而很难集中——这和一个只在短文本上训练过的模型，很难胜任长链路逻辑推理任务，是同一个道理。

这个类比让我对自己最近"越来越静不下心、总忍不住打开短视频、计划一再拖延"的状态有了更清晰的理解：这不完全是意志力问题，而更像是一种被输入内容持续"训练"出来的注意力模式。想要扭转它，一是要**有意识地调整输入**（减少碎片化刺激，逐步增加长文本、长时间专注的内容），二是要**把任务拆解到足够小的颗粒度**（比如把一本要看十小时的书拆成每半小时两页），用可执行的小步子去重新训练自己的专注力，而不是指望靠一时的自律去对抗已经被"调好参数"的习惯。

---

**小结**：这一路的思考，从 Transformer 的立体/平面结构问题出发，逐步延展到大模型与人脑在连接方式、记忆机制、学习方式、能耗模式上的根本差异，再落到存算一体、脉冲神经网络这些可能突破现有瓶颈的技术路径，又借果蝇大脑仿真实验看到了"结构 + 计算"与"学习机制"之间的鸿沟，最终归纳出类脑研究卡在结构、学习机制、硬件三大难题上、且三者必须协同突破的判断。而最后，这些关于"大脑如何被输入塑造"的认识，也反过来成了审视和调整自己注意力习惯的一面镜子。
